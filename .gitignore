node_modules/
.env
.next/
next-env.d.ts
*.tsbuildinfo
dist/
coverage/
# Salidas de Playwright (F0-14): trazas e informes de una corrida del e2e.
test-results/
playwright-report/
blob-report/
*.log
# Cliente de Prisma: lo genera `prisma generate` (postinstall), no se versiona (ADR 0008).
/src/adaptadores/prisma/generado/
# Mutation testing (F0-17): sandbox y reporte HTML de una corrida de Stryker.
/.stryker-tmp/
/reports/mutacion/
# Worktrees de agentes (M-01): no son código del repo, rompían `npm run lint` en el clon principal.
/.claude/
# Salida completa de cada paso de `npm run verificar` (M-01).
/.verificar/
