# src/app

Next.js (App Router). Solo habla con `casos-uso`, `puertos` e
`infraestructura`; nunca importa `dominio` directo para escribir (leer tipos
sí). El punto de armado (`infraestructura/arranque`) es la única excepción
para instanciar adaptadores.

Vacío hasta F0-04.
