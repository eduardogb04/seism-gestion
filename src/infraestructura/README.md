# src/infraestructura

Entorno (validación con Zod), log estructurado (pino), arranque: el único
lugar donde se arma la aplicación conectando puertos con adaptadores
concretos.

Vacío hasta F0-04 (`entorno.ts`, la primera variable validada).
