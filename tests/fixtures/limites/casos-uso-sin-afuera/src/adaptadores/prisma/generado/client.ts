// Hace de cliente generado por Prisma (F0-08): en el repo real vive en
// `src/adaptadores/prisma/generado/` y lo escribe `prisma generate`.
export class PrismaClient {
  readonly conectado = false;
}
